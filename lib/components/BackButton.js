import React from 'react';
import Icon from 'react-native-vector-icons/FontAwesome';


export default ({ size, onPress }) => (
  <Icon
    name="arrow-left"
    size={size}
    onPress={onPress}
  />
);

